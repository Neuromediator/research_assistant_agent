"""Local CLI entry point. Invoked via `uv run crewai run` or `uv run run_crew`."""


def run() -> None:
    """Kick off the research Flow. Implemented in Step 6 of SPEC §13."""
    raise NotImplementedError("ResearchFlow not yet implemented — see SPEC.md §13 step 6.")
